# Codex Sessions v0.2.3 Release Gate

Date: 2026-08-16

Result: **PASS — ready to publish from `dev`**

## Scope evidence

- The RED test reproduced the prior behavior: a target-project request selected and enriched both
  the target and a newer unrelated-project session.
- The project filter now runs in the candidate SQL before ordering, `--limit`, parsing, budgeting,
  API generation, or writes.
- Command-level synthetic coverage observes one candidate for the default current-project scope,
  one for explicit `--project`, and two only with explicit `--all`.
- Conflicting `--all` and `--project` flags fail before opening the index or API client.
- A real-corpus dry-run from `vibe-trading-jy` reports exactly its three sessions for both the
  default and explicit-project forms. An explicit global dry-run remains available and makes zero
  requests and writes.

## Verification

- `gofmt`, `golangci-lint run`, `go test ./... -count=1`, `go vet ./...`, `go build ./...`, and
  `git diff --check` pass.
- No live API request was needed for this gate; the generator boundary is covered by a synthetic
  recorder and the real-corpus checks used dry-run only.

## TDD effect record

Feature record: `20260816T060431Z-53b7023d`.

RED was observed and accurate, directly catching the cross-project selection issue. Focused GREEN,
command-level scope coverage, full-suite validation, and real dry-runs all passed with low workflow
friction.
