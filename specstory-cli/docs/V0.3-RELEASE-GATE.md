# Codex Sessions v0.3.1 Release Gate

Date: 2026-08-16

Behavioral merge commit: `c94feb5`

Result: **PASS — `v0.3.1` published from `dev` on 2026-08-16**

Release: <https://github.com/9penny/codex-sessions/releases/tag/v0.3.1>

The inherited Git history already contains a `v0.3.0` tag dated 2025-07-02. It is preserved rather
than overwritten, so the first available non-destructive Codex Sessions v0.3 tag is `v0.3.1`.

## Scope evidence

- The RED regression test proved enumeration previously exposed no distinction between an empty
  native inventory and provider failure or panic.
- Enumeration now carries explicit completeness per provider. Valid partial results remain
  indexable, but incomplete inventories cannot trigger cleanup.
- Individual file scan failures return valid partial references plus an aggregate error, closing
  the case where one malformed or temporarily unreadable JSONL could otherwise look deleted.
- The session index removes only absent live rows for the completed provider, together with FTS
  and AI metadata in one transaction. Tombstones and other providers remain intact.
- A command-level synthetic test indexes a temporary native Codex session, removes its fixture,
  reindexes, and proves the derived row disappears.
- Local-only list and project-browser tests prove `d delete` and `p filter projects` are visible.

## Real-corpus dogfood

- The development binary enumerated 97 native Codex sessions without a scan error.
- The first reindex removed 2 stale live index rows left behind by native deletion.
- An immediate second reindex found the same 97 native sessions, removed 0 rows, and reported the
  entire index up to date.
- Only aggregate command output was observed. No transcript, session ID, native path, AI metadata,
  credential, or endpoint value was printed or recorded.
- Reindex made no API request and did not modify native Codex JSONL.

## Verification

- `gofmt -w .`, `go vet ./...`, `go test ./... -count=1`, `golangci-lint run`, `go build ./...`,
  `git diff --check`, and the release-configuration contract pass.
- GitHub Test, Lint, and Release snapshot checks passed on PR 27.
- The tag workflow verified the tag belongs to `dev` and published Linux x86_64 and arm64 archives
  plus `checksums.txt`.
- The public installer verified the x86_64 checksum and installed `Codex Sessions 0.3.1` both in a
  temporary directory and at `~/.local/bin/csessions`.
- The installed public binary enumerated the same 97 native sessions without removing any further
  stale row; it refreshed the one session that changed during release work.
- No dependency changed.

## TDD effect record

Feature record: `20260816T062617Z-7547369c`.

RED was observed and accurate for the missing complete-scan contract, atomic reconciliation, scan
failure propagation, and TUI hints. The workflow also caught the per-file partial-scan safety gap
during implementation. Focused GREEN, package tests, the full suite, and real dogfood passed with
medium workflow friction.
